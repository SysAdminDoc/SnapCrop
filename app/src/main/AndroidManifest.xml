<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 14+ partial photo access. Declaring this makes the "Select photos" grant a
         first-class state we can detect, so the screenshot monitor can warn that it needs full
         media access instead of silently seeing only the user-selected subset. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <application
        android:name=".SnapCropApp"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SnapCrop"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.SnapCrop">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SnapCrop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <receiver
            android:name=".SnapCropWidgetProvider"
            android:exported="false"
            android:label="@string/widget_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/snapcrop_widget_info" />
        </receiver>

        <activity
            android:name=".WebCaptureActivity"
            android:exported="false"
            android:process=":web_capture"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".DeviceFrameActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".CollageActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".StitchActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".LongScreenshotReviewActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".VideoClipActivity"
            android:exported="false"
            android:theme="@style/Theme.SnapCrop" />

        <activity
            android:name=".CropActivity"
            android:exported="true"
            android:theme="@style/Theme.SnapCrop"
            android:colorMode="hdr"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/vnd.snapcrop.project+json" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.snapcrop.project+json" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/json" />
            </intent-filter>
        </activity>

        <service
            android:name=".ScreenshotService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="monitor_new_screenshots_and_show_persistent_user_controls_for_crop_actions" />
        </service>

        <service
            android:name=".ScrollCaptureService"
            android:exported="true"
            android:label="@string/scroll_capture_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/scroll_capture_accessibility_service" />
        </service>

        <service
            android:name=".StepCaptureService"
            android:exported="true"
            android:label="@string/step_capture_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/step_capture_accessibility_service" />
        </service>

        <service
            android:name=".StepCaptureTileService"
            android:exported="true"
            android:icon="@drawable/ic_crop"
            android:label="@string/tile_step_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".MonitorTileService"
            android:exported="true"
            android:icon="@drawable/ic_crop"
            android:label="SnapCrop"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".LongScreenshotTileService"
            android:exported="true"
            android:icon="@drawable/ic_crop"
            android:label="@string/tile_long_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".LastActionTileService"
            android:exported="true"
            android:icon="@drawable/ic_crop"
            android:label="Last action"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".FloatingScreenshotService"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ShareTargetReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
